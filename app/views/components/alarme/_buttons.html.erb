<% if channels.any? { |ch| ch['tipo'] == 'button' } %>
  <br>
  <div style="border: 1px solid lightblue; border-radius: 10px;">
    <div class="row text-center d-flex align-items-center justify-content-center" style="height: 100%; margin-botton: 15px;">
      <% channels.each do |ch| %>
        <% if ch.tipo == 'button' %>
          <div class="col">
            <%= render 'components/alarme/button_on', device: device, btn: ch %>
          </div>
          <div class="col">
            <%= render 'components/alarme/button_of', device: device, btn: ch %>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
<% end %>